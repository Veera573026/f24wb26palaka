block content
  h1 Index file
  p Welcome to Index.pug file
  button(type="button", class="btn btn-secondary", onclick="boost()") Press Me
  div#boost add in extra 

  script.
    let value = 0;
    let amt = 3;

    function boost(){
      amt += 1;
      value += amt;
      document.getElementById('boost').innerText = `Boosted ${value} by ${amt}`;
    }
