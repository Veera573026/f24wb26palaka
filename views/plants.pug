//- plants.pug
block content
  h1 Search Results - Plants
  p Here are some plants:

  //- Outer container div
  .container-fluid
    //- Row for results
    .row
      //- Iterate through the results array passed from the server
      each plant in results
        .col-sm-2
          h3 #{plant.plant_name}
          p Type: #{plant.plant_type}
          p Age: #{plant.plant_age} years
          //- Button that calls the buy function with the plant's name
          button(onclick="buy('#{plant.plant_name}')") Buy this plant
          
//- Add a script to handle the buy function
script.
  function buy(plantName) {
    alert("You purchased: " + plantName);
  }
